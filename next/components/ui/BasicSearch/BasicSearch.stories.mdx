import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { BasicSearch } from './BasicSearch'

<Meta title="components/molecules/BasicSearch" decorators={[withDesign]} />

# BasicSearch

BasicSearch

## Props

BasicSearch accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component
- <b>title</b>: String that is the title of the newsletter
- <b>buttonContent</b>: String that is written inside the button

<Canvas>
  <Story
    name="BasicSearch"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{ className: '' }}
  >
    {BasicSearch.bind({})}
  </Story>
</Canvas>

<ArgsTable of={BasicSearch} />
