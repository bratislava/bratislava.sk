import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import VenueList from './VenueList';

<Meta
  title="components/organisms/Sections/VenueList"
  decorators={[withDesign]}
/>

# VenueList

VenueList

<Canvas>
  <Story
    name="VenueList"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{}}
  >
    {VenueList.bind({})}
  </Story>
</Canvas>

<ArgsTable of={VenueList} />
