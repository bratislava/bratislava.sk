import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import StepTitle from './StepTitle'

<Meta title="components/molecules/StepTitle" decorators={[withDesign]} />

# StepTitle

StepTitle

## Props

StepTitle accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component
- <b>number</b>: number inside the circle
- <b>title</b>: title of the component

<Canvas>
  <Story
    name="StepTitle"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: 'space-x-5',
      number: 1,
      title: 'Lorem Ipsum',
    }}
  >
    {StepTitle.bind({})}
  </Story>
</Canvas>

<ArgsTable of={StepTitle} />
