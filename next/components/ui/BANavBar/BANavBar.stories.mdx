import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { BANavBar } from './BANavBar'

<Meta title="components/molecules/BANavBar" decorators={[withDesign]} />

# BANavBar

BANavBar

## Props

BANavBar accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component

<Canvas>
  <Story
    name="BANavBar"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
    }}
  >
    {BANavBar.bind({})}
  </Story>
</Canvas>

<ArgsTable of={BANavBar} />
