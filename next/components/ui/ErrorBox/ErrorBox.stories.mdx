import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { ErrorBox } from './ErrorBox'

<Meta title="components/molecules/ErrorBox" decorators={[withDesign]} />

export const Template = (props) => (
  <div className="p-12">
    <ErrorBox {...props} />
  </div>
)

# ErrorBox

ErrorBox

## Props

ErrorBox accepts custom Props that are as follows:

- <b>error</b>: Error object or string message

<Canvas>
  <Story
    name="ErrorBox"
    args={{
      error: 'Something went wrong',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ErrorBox} />
