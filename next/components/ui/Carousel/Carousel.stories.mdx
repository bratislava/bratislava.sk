import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'

import { Carousel } from './Carousel'
import { HorizontalCard } from '../HorizontalCard/HorizontalCard'

import NewsImage from '../../../assets/images/sample-news-image.jpeg'

export const Template = (props) => (
  <div className="m-20">
    <Carousel {...props} />
  </div>
)

<Meta title="components/molecules/Carousel" decorators={[withDesign]} />

# Carousel

Carousel

<Canvas>
  <Story
    name="Carousel"
    args={{
      items: Array(10)
        .fill(1)
        .map((_, i) => (
          <div key={i} className="flex-1 py-5 px-5 flex">
            <HorizontalCard className="w-full" key={i} imageSrc={NewsImage}>
              {`ITEM ${i}`}
            </HorizontalCard>
          </div>
        )),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Carousel} />
