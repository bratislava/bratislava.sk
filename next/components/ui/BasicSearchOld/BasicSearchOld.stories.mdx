import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { BasicSearchOld } from './BasicSearchOld'

<Meta title="components/molecules/BasicSearchOld" decorators={[withDesign]} />

# BasicSearchOld

BasicSearchOld

## Props

BasicSearchOld accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component
- <b>title</b>: String that is the title of the newsletter
- <b>buttonContent</b>: String that is written inside the button

<Canvas>
  <Story
    name="BasicSearchOld"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{ className: '' }}
  >
    {BasicSearchOld.bind({})}
  </Story>
</Canvas>

<ArgsTable of={BasicSearchOld} />
