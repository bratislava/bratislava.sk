import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { Waves } from './Waves'

export const argTypes = {
  wavePosition: {
    control: {
      type: 'radio',
      options: ['top', 'bottom'],
    },
  },
}

export const defaultArgs = {
  backgroundColor: 'var(--background-color)',
  waveColor: 'var(--category-color-100)',
  wavePosition: 'top',
  isRich: true,
}

<Meta title="components/molecules/Waves" decorators={[withDesign]} />

# Waves

Waves should be used in some background container i.e. SectionContainer. You can use "transparent" for colors to get transparent sections.

## Wave with basic configuration

<Canvas>
  <Story
    name="Basic configuration"
    argTypes={argTypes}
    args={{
      ...defaultArgs,
      wavePosition: 'top',
    }}
  >
    {Waves.bind({})}
  </Story>
</Canvas>

## Wave with detailed configuration

<Canvas>
  <Story
    name="Detailed configuration"
    argTypes={argTypes}
    args={{
      ...defaultArgs,
      waveColor: 'lightgreen',
      backgroundColor: 'lightblue',
      outerLinesColor: 'darkblue',
      innerLinesColor: 'darkgreen',
    }}
  >
    {Waves.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Waves} />
