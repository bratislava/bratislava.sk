import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { Select } from './Select';

<Meta title="components/atoms/Select" decorators={[withDesign]} />

# Select

Styled select, that is used as a default select throughout all Bratislava web apps.

## Props

CheckBox accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component
- <b>selectClassName</b>: className for the Select Component
- <b>options</b>: options for the select of generic type T
- <b>onChange</b>: function, accepts as parameter selected option as object
- <b>value</b>: selected option's value
- <b>id</b>: identifier of the Select Component
- <b>hasError</b>: Boolean value, that indicates, that the Select should have
  error styling (red border)
- <b>hasIcon</b>: Boolean value, that indicates, that the Select has an icon
  (icon is hardcoded ChevronDown positioned right of the value)

export const Template = (args) => {
  const [value, setValue] = React.useState('0');
  return (
    <Select
      className={args.className}
      options={args.options}
      hasError={args.hasError}
      hasIcon={args.hasIcon}
      value={value}
      onChange={(d) => setValue(d)}
    />
  );
};

<Canvas>
  <Story
    name="Select"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
      selectClassName: ' ',
      hasError: false,
      hasIcon: true,
      options: [
        { key: '0', title: 'Option 1' },
        { key: '1', title: 'Option 2' },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Select} />
