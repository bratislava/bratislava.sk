import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { ImageCard } from './ImageCard';
import RentImg from '../../assets/images/news.svg';

<Meta title="components/atoms/ImageCard" decorators={[withDesign]} />

# ImageCard

This is our ImageCard

<Canvas>
  <Story
    name="ImageCard"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    argTypes={{
      imagePosition: {
        control: {
          type: 'radio',
          options: ['left', 'right'],
        },
      },
    }}
    args={{
      imageSrc: RentImg,
      footerText: 'Kapacita priestoru',
      smallGapCapacity: 120,
      bigGapCapacity: 60,
    }}
  >
    {ImageCard.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ImageCard} />
