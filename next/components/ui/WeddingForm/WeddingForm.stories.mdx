import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { WeddingForm } from './WeddingForm'

<Meta title="components/molecules/WeddingForm" decorators={[withDesign]} />

# WeddingForm

WeddingForm

## Props

WeddingForm accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component

<Canvas>
  <Story
    name="WeddingForm"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
    }}
  >
    {WeddingForm.bind({})}
  </Story>
</Canvas>

<ArgsTable of={WeddingForm} />
