import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { NewsLetter } from './NewsLetter';
import NewsletterImage from '../../assets/images/newsletter-image.png';

<Meta title="components/molecules/NewsLetter" decorators={[withDesign]} />

# NewsLetter

NewsLetter

## Props

NewsLetter accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component
- <b>title</b>: String that is the title of the newsletter
- <b>buttonContent</b>: String that is written inside the button

<Canvas>
  <Story
    name="NewsLetter"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
      imageSrc: NewsletterImage,
      title: 'Odoberajte novinky z knižnice',
      buttonContent: 'Subscribe',
    }}
  >
    {NewsLetter.bind({})}
  </Story>
</Canvas>

<ArgsTable of={NewsLetter} />
