import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import { StepperSpacer } from './StepperSpacer';

<Meta title="components/atoms/StepperSpacer" decorators={[withDesign]} />

# StepperSpacer

Styled Line, that is used 'as' a StepperSpacer 'for' our Stepper Component in OrderSteps.

## Props

StepperStep accepts custom Props that are as follows:

- <b>active</b> Boolean, that indicates the active state of the Component

<Canvas>
  <Story
    name="StepperSpacer"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      active: false,
    }}
  >
    {StepperSpacer.bind({})}
  </Story>
</Canvas>

<ArgsTable of={StepperSpacer} />
