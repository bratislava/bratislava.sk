import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';
import PageLinkButton from './PageLinkButton';

<Meta title="components/PageLinkButton" decorators={[withDesign]} />

# PageLinkButton

This is our PageLinkButton

## Single PageLinkButton

TODO: Write Docs

<Canvas>
  <Story
    name="Single PageLinkButton"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{}}
  >
    {PageLinkButton.bind({})}
  </Story>
</Canvas>

<ArgsTable of={PageLinkButton} />
