import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { TableRow } from './TableRow'

<Meta title="components/atoms/TableRow" decorators={[withDesign]} />

# TableRow

TableRow

## Props

TableRow accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component
- <b>tableCells</b>: array of table cells, each table cell has individual content and classname

<Canvas>
  <Story
    name="TableRow"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
      tableCells: [
        { content: 'Adam@gmail.com', className: 'rounded-l-lg' },
        { content: 'Mrkovicka', className: '' },
        { content: 'Adam', className: '' },
        { content: '#1', className: 'rounded-r-lg' },
      ],
    }}
  >
    {TableRow.bind({})}
  </Story>
</Canvas>

<ArgsTable of={TableRow} />
