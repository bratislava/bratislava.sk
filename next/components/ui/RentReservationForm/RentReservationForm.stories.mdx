import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { RentReservationForm } from './RentReservationForm'

<Meta title="components/molecules/RentReservationForm" decorators={[withDesign]} />

# RentReservationForm

RentReservationForm

## Props

RentReservationForm accepts custom Props that are as follows:

- <b>className</b>: modifies or extends the styles for the component

<Canvas>
  <Story
    name="RentReservationForm"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{
      className: '',
    }}
  >
    {RentReservationForm.bind({})}
  </Story>
</Canvas>

<ArgsTable of={RentReservationForm} />
