import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { withDesign } from 'storybook-addon-designs';

import { VerticalCard } from './VerticalCard';
import { VerticalCardButton } from '../VerticalCardButton/VerticalCardButton';

import SampleImage from '../../assets/images/sample-blog-image.png';

export const defaultArgs = {
  // className: 'w-64',
  children: 'Verejný priestor na Kazanskej ulici vo Vrakuni sa dočká obnovy',
};

<Meta title="components/molecules/VerticalCard" decorators={[withDesign]} />

# Vertical card

## Card with content

<Canvas>
  <Story
    name="With content"
    args={{
      ...defaultArgs,
    }}
  >
    {VerticalCard.bind({})}
  </Story>
</Canvas>

## Card with content and image

<Canvas>
  <Story
    name="With image"
    args={{
      ...defaultArgs,
      imageSrc: SampleImage,
    }}
  >
    {VerticalCard.bind({})}
  </Story>
</Canvas>

## Card with content, image and accessory

<Canvas>
  <Story
    name="With accessory"
    args={{
      ...defaultArgs,
      imageSrc: SampleImage,
      accessory: <VerticalCardButton />,
    }}
  >
    {VerticalCard.bind({})}
  </Story>
</Canvas>

## Card as used in designs

<Canvas>
  <Story
    name="With specified sizes"
    args={{
      ...defaultArgs,
      className: 'w-64',
      imageSrc: SampleImage,
      accessory: <VerticalCardButton />,
    }}
  >
    {VerticalCard.bind({})}
  </Story>
</Canvas>

<ArgsTable of={VerticalCard} />
