import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { withDesign } from 'storybook-addon-designs'
import { BlogCard } from './BlogCard'

<Meta title="components/molecules/BlogCard" decorators={[withDesign]} />

# BlogCard

BlogCard

<Canvas>
  <Story
    name="BlogCard"
    parameters={{
      design: {
        type: 'figma',
        url: '',
      },
    }}
    args={{}}
  >
    {BlogCard.bind({})}
  </Story>
</Canvas>

<ArgsTable of={BlogCard} />
