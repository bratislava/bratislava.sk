version: '3.8'
services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres4
      PGPORT: 5434
    expose:
      - '5434'
    ports:
      - '127.0.0.1:5434:5434'

volumes:
  postgres:
